index_generator:
  exclude:
    - "**.g.dart"
    - "**.freezed.dart"
    - "**.gr.dart"
    - "**.tailor.dart"
    - "**.part.dart"

  # Define the paths of the folders in which to generate the index files
  indexes:
    - path: app/lib
      name: widgetbook_helper
